/* eslint-disable linebreak-style */
/* eslint-disable no-console */
import VapiLoader from '../vapi';
import logger from '../logger';

function Assistant() {
  logger.log('>>> Assistant component rendered');
  return (
    <VapiLoader />
  );
}

export default Assistant;
